header
  nav.navbar.navbar-expand-lg.navbar-dark.bg-primary
    .container
      a.navbar-brand(href="/") #{helpers.siteName}
      button.navbar-toggler(type="button", data-toggle="collapse", data-target="#navbarColor01", aria-controls="navbarColor01", aria-expanded="false", aria-label="Toggle navigation")
        span.navbar-toggler-icon
      #navbarColor01.collapse.navbar-collapse
        ul.nav.navbar-nav.ml-auto
          if user
            li.nav-item
              a.nav-link(href="/profile") My Profile
            li.nav-item
              a.nav-link(href="/logout") Logout
          else
            li.nav-item
              a.nav-link(href="/login") Sign in
            li.nav-item
              a.nav-link(href="/register") Sign up

